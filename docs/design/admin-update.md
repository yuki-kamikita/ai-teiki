## 管理者の手動更新（推奨プラン）

### 目的
- 週1回の更新を「確実に」「安全に」実行する。

### 画面でできること（最小）
- 次回の更新予定時刻の表示
- 更新実行ボタン（確認ダイアログ付き）
- 更新中のステータス表示（実行中/完了/失敗）
- 直近の更新ログを表示

### 安全のためのルール（最小）
- 連続実行を防ぐ（同時実行を禁止）
- 失敗時は「失敗ログ」を保存する
- 実行前に「本当に実行するか」確認する

### 追加であると良いもの（余裕があれば）
- 更新の延期/再実行
- 簡易な事前チェック（前回からの間隔など）
- 実行前のバックアップ
